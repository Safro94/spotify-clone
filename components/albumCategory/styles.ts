import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        margin: 10
    },

    title: {
        color: 'white',
        'font-size': 30,
        'font-weight': 'bold',
    }
})

export default styles;